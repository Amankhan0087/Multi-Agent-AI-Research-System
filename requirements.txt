# ============================================================
# Multi-Agent Research System - Requirements
# ============================================================

# ------------------------------------------------------------
# Web Scraping & Data Extraction Tools
# ------------------------------------------------------------
tavily-python>=0.3.0          # Tavily search/research tool (Tool 1)
beautifulsoup4>=4.12.0        # BeautifulSoup HTML/XML parsing (Tool 2)
requests>=2.31.0              # HTTP requests for web fetching
lxml>=4.9.0                   # Fast XML/HTML parser (BeautifulSoup backend)
httpx>=0.25.0                 # Async HTTP client

# ------------------------------------------------------------
# LLM Backends (Groq primary → Gemini fallback)
# ------------------------------------------------------------
groq>=0.9.0                   # Groq API client (primary)
langchain-groq>=0.1.0         # LangChain integration for Groq (Llama 3.3 70B)
google-genai>=1.0.0           # Google Gemini API client (fallback)
langchain-google-genai>=4.0.0 # LangChain integration for Gemini (auto-fallback)

# ------------------------------------------------------------
# LangGraph / Runnables (Pipeline Orchestration)
# ------------------------------------------------------------
langgraph>=0.1.0              # Multi-agent graph orchestration with Runnables
langchain>=0.2.0              # Core LangChain (Runnables, Tools, Prompts)
langchain-core>=0.2.0         # LangChain Runnables & LCEL pipeline primitives

# ------------------------------------------------------------
# React Agent
# ------------------------------------------------------------
# React (Reasoning + Acting) agent is built using LangGraph
# and LangChain's create_react_agent — no separate package needed
# Requires: langchain, langchain-groq, langgraph (listed above)

# ------------------------------------------------------------
# Environment & Configuration
# ------------------------------------------------------------
python-dotenv>=1.0.0          # Load API keys from .env file
pydantic>=2.0.0               # Data validation and settings management

# ------------------------------------------------------------
# Frontend
# ------------------------------------------------------------
streamlit>=1.35.0             # Web UI framework for the research dashboard

# ------------------------------------------------------------
# Utilities
# ------------------------------------------------------------
typing-extensions>=4.8.0      # Extended type hints support
tenacity>=8.2.0               # Retry logic for API calls
rich>=13.0.0                  # Rich terminal formatting and output styling